<%= form_for @user, url: login_path, method: :post do |f| %>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "●", class: "like-button" %>
  Don’t have an account? <%= link_to(controller: 'users', action: 'new') { 'Sign up' } %>
<% end %>
